# =============================================================================
# JIP MF Recommendation Engine — Python Dependencies
#
# Install with:  pip install -r requirements.txt
# =============================================================================

# -- Web framework --
fastapi==0.115.0
uvicorn[standard]==0.30.6

# -- Database (async PostgreSQL) --
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.13.2

# -- Validation & settings --
pydantic==2.9.2
pydantic-settings==2.5.2
eval-type-backport>=0.2.0

# -- HTTP client (Morningstar API) --
httpx==0.27.0

# -- XML parsing (Morningstar responses) --
lxml>=5.0.0

# -- Structured logging --
structlog==24.4.0

# -- Background job scheduling --
apscheduler==3.10.4

# -- Data processing --
pandas==2.2.3
openpyxl==3.1.5

# -- Auth --
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# -- Rate limiting --
slowapi==0.1.9

# -- Environment --
python-dotenv==1.0.1
